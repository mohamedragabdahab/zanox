imports:
    - { resource: config_dev.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

swiftmailer:
    disable_delivery: true

#database config
parameters:
    database_driver: pdo_mysql
    database_host: 127.0.0.1
    database_port: ''
    database_name: zanox_test
    database_user: root
    database_password: root